spring:
  datasource:
    url: jdbc:h2:mem:youArts_request_db
    username: lucas
    password: 1234
    driver-class-name: org.h2.Driver
    initialization-mode: always

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  flyway:
    enabled: false